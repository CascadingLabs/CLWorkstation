---
- name: Apply Cascading Labs workstation loadout
  hosts: workstations
  become: false
  gather_facts: true

  pre_tasks:
    - name: Load distro-specific vars
      ansible.builtin.include_vars: "{{ item }}"
      with_first_found:
        - "vars/{{ ansible_facts['os_family'] }}.yml"
        - "vars/default.yml"
      tags: always

    - name: Load tool catalog
      ansible.builtin.include_vars: vars/tools.yml
      tags: always

    - name: Load font catalog
      ansible.builtin.include_vars: vars/fonts.yml
      tags: always

  roles:
    - role: common
      tags: [common]
    - role: tools
      tags: [tools]
    - role: fonts
      tags: [fonts]
    - role: shell
      tags: [shell]
    - role: neovim
      tags: [neovim]
    - role: tailscale
      tags: [tailscale]
      when: enable_tailscale | bool
